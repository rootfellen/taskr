import React from "react";
import { LoaderWrapper, Loader } from "./LoaderElements";

const s = () => {
  return (
    <LoaderWrapper>
      <Loader />
    </LoaderWrapper>
  );
};

export default s;
